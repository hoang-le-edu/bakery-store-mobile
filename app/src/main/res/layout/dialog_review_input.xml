<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_lg"
        android:text="Write a Review"
        android:textColor="@color/black"
        android:textSize="@dimen/text_subsubtitle"
        android:textStyle="bold" />

    <RatingBar
        android:id="@+id/ratingBar_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:progressTint="@color/yellow_star"
        android:secondaryProgressTint="@color/yellow_star"
        android:indeterminateTint="@color/yellow_star"
        android:numStars="5"
        android:stepSize="1"
        android:rating="3" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/reviewTextInputLayout"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_lg"
        app:boxBackgroundMode="outline"
        android:hint="Share your experience...">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etReviewText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minLines="4"
            android:gravity="top"
            app:boxCornerRadiusTopStart="@dimen/space_sm"
            app:boxCornerRadiusTopEnd="@dimen/space_sm"
            app:boxCornerRadiusBottomStart="@dimen/space_sm"
            app:boxCornerRadiusBottomEnd="@dimen/space_sm"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Media Upload Section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_lg"
        android:text="Add Photos/Videos (Optional)"
        android:textColor="@color/black"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Maximum 5 files, 10MB each. Supported: JPG, PNG, GIF, MP4, MOV, AVI"
        android:textColor="@android:color/darker_gray"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:orientation="horizontal">

        <!-- Add Media Button -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardAddMedia"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_margin="4dp"
            android:clickable="true"
            android:focusable="true"
            app:cardCornerRadius="8dp"
            app:cardElevation="2dp"
            app:rippleColor="@color/colorPrimary">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:background="?attr/selectableItemBackground">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_add"
                    app:tint="@color/colorPrimary" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="Add"
                    android:textColor="@color/colorPrimary"
                    android:textSize="10sp" />

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Uploaded Media RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvUploadedMedia"
            android:layout_width="0dp"
            android:layout_height="88dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:layout_marginStart="8dp" />

    </LinearLayout>
    
    <!-- Read-only TextView for viewing existing review -->
    <TextView
        android:id="@+id/tvReadOnlyReview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_lg"
        android:visibility="gone"
        android:text="This is the review text."
        android:lineSpacingExtra="4dp"
        android:textSize="@dimen/text_md"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="24dp"
        android:gravity="end">

        <!-- Delete Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDeleteReview"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Delete"
            android:textColor="@color/colorRedButton"
            android:visibility="gone"/>

        <!-- Edit/Submit Buttons -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnEditReview"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Edit"
            android:visibility="gone"
            android:layout_marginEnd="@dimen/space_md"/>
            
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSubmitReview"
            android:backgroundTint="@color/red_add_button"
            app:cornerRadius="50dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submit"/>
            
    </LinearLayout>
</LinearLayout>
